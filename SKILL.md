---
name: medical-slide-spec
description: 将医学客户资料整理为可直接放入PPT的逐页完整文案，按风险判定证据等级并对高风险或科研主张执行联网核验门禁，再在客户明确确认后生成内容完全同源的客户终版与视觉AI投喂版。适用于护理查房、教学查房、护理个案、PDCA、品管圈、RCA、患者宣教、护理培训、科研开题/答辩、述职竞聘、循证转化和新技术项目。用户提到医学PPT文字大纲、逐页文案、客户确认稿、文献核验、内容冻结、content_lock_id、Kimi/ChatGPT做PPT前的文字准备、资料不全补齐或PPT内容审核时使用。只负责内容结构、医学闭环、完整可见文字、数据、来源、真实性、证据门禁和内容密度；不分析或规定配色、字体、布局、页面原型、容器、图标和装饰，不承诺最终视觉效果、医学终审或客户验收。
---

# 医学PPT逐页内容冻结

当前规则版本：`v12.3 便携发布版`。

大纲路由继续使用`v11.0 大纲路由`和14张既有大纲卡；v12系列只替换逐页内容与冻结合同。

## 核心职责

执行以下链路：

`客户资料 → 医学内容整理 → 证据分级与核验 → 逐页完整文案 → 客户确认 → 内容冻结`

把视觉阶段留给下游：

`冻结内容 + 当次参考模板/效果图 → Kimi、ChatGPT或其他视觉AI自行设计`

当本次提供参考PPT或效果图时，将其作为唯一视觉效果依据。视觉AI必须学习参考稿的视觉语言、
构图方式、信息层级、页面节奏和整体质感，不得另行建立、混合或套用其他视觉风格。

不要兼任PPT设计师。不要输出模板画像、视觉设计系统、版式选择、颜色、字体、区域比例、
页面原型、容器策略、图标预算、连接线或装饰说明。若内容包中意外残留这些要求，视觉AI必须
忽略，且校验器应阻止冻结包通过。

## 两阶段交付

### 阶段A：客户确认草稿

首次只生成一份UTF-8纯文本：

`YYYYMMDD_项目名_PPT逐页内容_客户确认版_vXX.txt`

写全每一页最终可见的标题、正文、核心结论、表格单元格、图表数据、来源脚注，以及不进入
PPT的内容性质和待确认项。客户确认稿不是概要，不能只写“介绍病例”“展示护理措施”。

将状态写为`待确认`，`content_lock_id`写为`待确认`。不得生成视觉AI投喂终版。

### 阶段B：内容冻结

只有使用者明确转述客户已确认，例如“内容确认，可以制作”“按这版做”，才冻结。模糊表述、
样稿讨论、局部认可和“先看看”均不算确认。

冻结前逐项处理所有待确认项，只允许：

1. 用真实资料替换；
2. 客户明确接受当前建议、模拟或未记录表述；
3. 删除该内容。

同时将`未完成证据项总数`清零，并把`证据冻结状态`写为`可冻结`。E2、E3项目若未完成当次
联网核验，即使客户接受文字也不得冻结；客户确认不能替代证据核验。

然后使用`scripts/freeze_content_package.py`从客户确认稿生成同版本的两份终版：

- 客户确认终版；
- 视觉AI投喂版。

两份终版必须具有相同的`content_lock_id`和逐页可见内容。任何文字、数字、单位、来源、页序
或表格单元格发生变化，都建立新版本和新内容锁；不要覆盖旧版本。

## 输入优先级

优先读取未来上游Skill产生的：

1. 《需求确认单》；
2. 《资料预检清单》；
3. 原始病例、护理记录、比赛要求、客户大纲和其他来源资料。

标准输入字段见`references/input-contract.md`。没有标准包时，先读完现有资料，再按
`references/intake-requirements.md`做最小补问。不要询问视觉风格，也不要因缺少模板而停止。

参考文件只判断用途：

- `视觉参考`：不分析其效果，只记录由使用者另行提供给下游视觉AI；
- `内容结构参考`：可提取目录、页数、比赛规则、必须章节和文字要求；
- `二者都有`：只处理其中的内容结构部分，视觉部分仍不分析。

## 内容边界

### 保留并执行

- 任务类型、场景、受众、目的、页数和汇报时长；
- v11大纲卡、显式目录和页数分配；
- 每页唯一内容任务与完整可见文案；
- 表格全部列名、行名和单元格；
- 图表所需分类、系列、数值、单位、时间、样本量、口径和来源；
- 客户事实、联网证据、建议补全、模拟内容和待提供内容的边界；
- E0–E3证据等级、联网许可、逐页证据状态和未完成证据项；
- 医学闭环、隐私、来源和人工审核要求；
- 只基于文字量和数据完整性的内容密度门禁。

### 明确退出

- 模板视觉画像和无模板风格推荐；
- 配色、字体、字号、背景、网格和留白；
- 布局比例、页面原型、容器、分区、图标、连线和视觉焦点；
- 动画、缩略图、成图、渲染后视觉回查；
- 对Kimi或ChatGPT最终视觉执行效果的保证。

## 必读与按需读取

每次任务完整读取：

- `references/input-contract.md`
- `references/intake-requirements.md`
- `references/content-completion.md`
- `references/evidence-policy.md`
- `references/content-spec-contract.md`
- `references/content-density-governance.md`
- `references/content-lock-contract.md`
- `references/quality-gates.md`

需要选择大纲时读取`references/outline-routing.md`和`references/outline-card-contract.md`，再只读取
`references/outline-cards/`中命中的一张卡。14张卡与基础内容路由的映射以
`references/outline-cards/manifest.json`为真源。

需要四类通用专业闭环时读取`references/routes.md`；14张卡无匹配时再读取
`references/standard-outlines.md`。

客户草稿使用`assets/customer-content-template.md`；缺少上游标准包时使用
`assets/input-packet-template.md`；冻结后的视觉AI版由脚本按
`assets/visual-ai-handoff-template.md`的边界生成。有参考时必须声明参考稿是唯一视觉效果依据、
忽略文字包残留视觉要求并主动学习参考视觉；无参考时不得推荐或推断通用风格。

## 工作流

### 1. 建立任务目录与版本

优先遵循当前工作区`AGENTS.md`、用户指定路径或宿主环境的输出规则。若三者均未规定，默认在
当前可写工作区创建`outputs/medical-slide-content/YYYYMMDD_任务名/`，临时文件放在同一工作区的
`tmp/medical-slide-spec/`。不要假定盘符、用户名、操作系统或固定的Codex安装目录。

保留原始材料只读，不要覆盖旧版本。开始调用脚本前，将当前`SKILL.md`所在目录解析为
`<SKILL_DIR>`；后续脚本均从该目录的`scripts/`调用。使用环境中可用的Python 3启动命令：
`python`、`python3`或Windows上的`py -3`，不要写死本机解释器路径。

### 2. 读取资料并补齐内容要求

先提取已有信息，再只补问会改变内容的缺口：主题、结果、场景、受众、页数/时长、客户大纲、
必须章节、禁止改写内容、补全权限、隐私、联网许可、必须核验的医学主张、审核人和交付时间。

不要询问颜色、字体、风格、模板效果或版式。参考图是否存在不构成内容生成门禁。

### 3. 确定补全模式

默认采用`授权补全`。按`references/content-completion.md`区分：

- 客户已提供；
- 联网核验；
- 建议补全-待核实；
- 完整模拟；
- 待客户提供。

不要把建议或模拟内容冒充为客户事实。当前指南、剂量、阈值、禁忌和操作仍需核验。

### 4. 判定证据等级与联网门禁

按`references/evidence-policy.md`判定E0–E3。普通项目默认E1，但出现现行指南、剂量、阈值、
禁忌、操作、器械或公开统计时至少升级为E2；科研开题、科研答辩和循证转化默认E3。

- E0：只有客户事实或无外部医学主张，不要求联网；
- E1：稳定常规医学内容，可用客户提供且可核查的现行权威资料，联网按需；
- E2：时效性或高风险医学主张，冻结前强制完成当次联网核验；
- E3：科研或强循证任务，冻结前强制联网，并建立核心主张—来源映射。

每次联网前必须取得当次许可并先去标识化。E2、E3未获许可时可以形成标明阻断原因的草稿，
但不得声称已核验、不得清零证据项、不得冻结。病例事实始终只来自客户记录或明确模拟，不能
用文献补造患者经历、项目过程或效果数据。

### 5. 确定显式大纲

按以下优先级确定目录：

1. 客户明确大纲；
2. 内容结构参考中的硬性目录；
3. 命中的v11大纲卡；
4. 四类兼容大纲；
5. 获得联网许可后形成的定制大纲。

专业闭环只用于填充和检查内容，不机械变成目录标题。先拟定章节页数，再逐页写作；内容过量时
删减低价值信息或在章节内拆页。全部页面通过内容密度门禁后才冻结页序。

### 6. 写逐页完整内容

按`references/content-spec-contract.md`逐页写：

- 第1页封面写主标题、可选副标题和署名信息；
- 第2页起用`第X页｜唯一可见标题`；
- 正文写最终句子和标签；
- 核心结论写实际要显示的一句话，不写制作备注；
- 表格写完整单元格，不使用时写`无`；
- 图表数据写全必要字段，不使用时写`无`；
- 来源脚注写实际显示或可核查来源；
- 内容说明记录内容性质、证据状态、待确认项和不得改动项，不进入PPT。

不要写任何视觉效果控制。若某页内容明显过量，按`references/content-density-governance.md`
删减或拆页；不要给出字号、布局或缩字建议。

### 7. 生成并校验客户草稿

按`assets/customer-content-template.md`生成客户确认稿，运行：

```text
python "<SKILL_DIR>/scripts/validate_content_package.py" draft "<客户确认稿.txt>" --route <route> --mode <mode> [--outline-card <card_id>]
```

草稿可以有待确认项和未完成证据项，但必须逐项具体列出，并明确`证据冻结状态：阻断`。
结构校验通过不等于完成文献核验、客户确认或医学终审。

### 8. 明确确认后冻结

收到明确确认后，先把所有逐页`待确认项`处理为`无`，把所有逐页`证据状态`处理为非`待核验`，
并确认`未完成证据项总数：0`、`证据冻结状态：可冻结`，再运行：

```text
python "<SKILL_DIR>/scripts/freeze_content_package.py" "<已确认客户稿.txt>" --customer-output "<客户终版.txt>" --ai-output "<视觉AI投喂版.txt>" --date YYYYMMDD --version vXX --confirmation "客户已明确确认" --visual-reference "另行提供／本次无参考" --route <route> --mode <mode> [--outline-card <card_id>]
```

脚本不得覆盖已有文件。生成后运行双文件校验：

```text
python "<SKILL_DIR>/scripts/validate_content_package.py" locked "<客户终版.txt>" --ai "<视觉AI投喂版.txt>" --route <route> --mode <mode> [--outline-card <card_id>]
```

只有双文件内容锁、逐页可见内容和版本全部一致时，才允许进入视觉制作阶段。

## 结论边界

始终分别记录：

`结构完整 ≠ 证据核验完成 ≠ 客户已确认 ≠ 内容已冻结 ≠ 医学审核通过 ≠ 视觉AI已正确执行 ≠ PPT可交付`

视觉参考变化不改变内容锁；冻结内容变化必须建立新版本和新锁。
